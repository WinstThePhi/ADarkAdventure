#include <windows.h>
#include "layer.h"

int 
WinMain(HINSTANCE hInstance, 
        HINSTANCE prevInstance,
        LPSTR CMDLine, 
        INT cmdShow)
{
    MessageBox(0, "Hello World", "The Dark Adventure", MB_OK);
    return 0;
}